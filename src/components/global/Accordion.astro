---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<details class="accordion-group group py-4 border-b border-slate-200 cursor-pointer overflow-hidden transition-all duration-500 rounded-xl px-4 hover:bg-slate-50">
  <summary class="flex items-center justify-between list-none font-semibold text-slate-800 text-lg hover:text-pink-600 transition-colors duration-300 py-2">
    <span>{title}</span>
    <span class="transition-transform duration-500 group-open:rotate-180 bg-white shadow-sm p-1 rounded-full">
      <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="w-5 h-5"><path d="m6 9 6 6 6-6"/></svg>
    </span>
  </summary>
  <div class="mt-3 text-slate-600 leading-relaxed overflow-hidden transition-all duration-300 glass p-4 rounded-lg mb-2">
    <slot />
  </div>
</details>

<style>
  /* Custom summary marker removal for Safari */
  summary::-webkit-details-marker {
    display: none;
  }
  
  .accordion-group[open] summary {
    color: #2563eb; /* blue-600 */
  }

  /* Smooth transition for details content */
  .accordion-group div {
    animation: slideDown 0.4s ease-out;
  }

  @keyframes slideDown {
    from {
      opacity: 0;
      transform: translateY(-8px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
